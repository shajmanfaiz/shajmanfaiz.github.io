module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'ar'],
  },
  
  // debug: process.env.NODE_ENV === 'development',
  // serialize config for json, simple objects is not allowed and throws error, 
  // you can either keep it commented or remove it.
  // localeExtension: 'true',
  // reloadOnPrerender: process.env.NODE_ENV === 'development',
};
